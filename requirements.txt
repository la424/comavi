# COMAVI Python dependencies (developed and verified on Python 3.11)
# Pin an exact environment with: pip freeze > requirements.lock
#
# Verification only needs pandas/numpy/scipy/openpyxl -- it reads the committed
# stored inputs. biopython and pyyaml are needed to run the pipeline itself
# (structure loading and config I/O). FoldX 5.1 is proprietary and must be
# supplied separately; see README.
pandas
numpy
scipy
openpyxl
biopython
pyyaml

# Required to regenerate reference_outputs/COMAVI_audit_gate_sweep.csv, whose
# Benjamini-Hochberg-adjusted p-values appear in the manuscript's gate-sweep
# table. Without it build_audit_corrections.py cannot run and those adjusted
# values are not reproducible from source.
statsmodels

# Required to verify the manuscript. verify_embedded_figures.py compares the
# figures embedded in the .docx against the files the generators write, and
# python-docx reads the manuscript text every prose audit checks. These were
# previously listed as optional presentation dependencies; they are not. The
# embedded-figure gate is the only check that can see a stale figure inside the
# document, and without these it cannot run at all.
pillow
python-docx

# Figure and deck builders only -- not needed to verify any result. Listed so a
# reader who wants to regenerate the presentation artifacts is not left guessing:
#   python-pptx  -> build_lab_meeting_deck.py, audit_deck_numbers.py

# PDB/mmCIF parsing and sequence-aware Colab system setup
gemmi
