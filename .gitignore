# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS
.DS_Store

# FoldX — proprietary; users supply their own binary (see README). Never commit.
foldx
foldx5*
foldx_[0-9]*
rotabase.txt
*.fxout

# AlphaFold structures — large; obtain from AlphaFold DB / Server (see docs/adding_your_own_genes.md)
structures/
**/structures/

# Large, regenerable pipeline outputs (recreated by running the pipeline)
**/foldx_runs/
processed/
results/chd_rerun/
results/foldx_runs/
results/aug4/
verification_run/
_work/

# Logs and archives
*.log
*.tar.gz

# self-test / example run byproducts
verification_output/

# Release archives staged for Zenodo upload (not tracked)
dist/

# AHA fellowship working files — unrelated to the COMAVI methods repo
AHA_docx_diagnostic/
AHA_harmonized_citations/
